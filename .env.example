# Copy to .env and fill in. .env is gitignored — never commit a real
# credential, and rotate any that has been shared in plain text.
#
# Read by core/drone_db.py for the shooter_drones lookup.
DRONE_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
